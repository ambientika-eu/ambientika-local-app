# 100% local Ambientika stack — NO cloud credentials required.
# (The upstream .env needs AMBIENTIKA_EMAIL / PASSWORD; this one does not.)

MQTT_PREFIX=ambientika

# Setup pushed to each unit on connect (single-master default).
HOUSE_ID=1
DEVICE_ROLE=0      # 0=MASTER, 1=SLAVE_EQUAL_MASTER, 2=SLAVE_OPPOSITE_MASTER
DEVICE_ZONE=0

LOG_LEVEL=INFO
